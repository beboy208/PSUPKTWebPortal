Views or Pages ==> components

a page can import a component to display it and do nothing else, not even Business Logic.

it’s possible to import Context Providers inside of your page so the child components can consume it.
